# 变更日志

## [Unreleased]

- 变更：主题预设改为内置样式，`appearance.active_preset` 由手动输入预设名称改为下拉框直接选择
- 新增：下拉框新增「自定义」选项，选择后使用下方预设列表中的自定义配色；列表默认提供 `zhenxun` 作为模板，卡片标题直接显示预设名
- 变更：预设列表默认仅保留 `zhenxun` 一套可编辑条目；所有内置预设配色锁定，列表条目仅作为「自定义」选项的配置源
- 变更：字体优先级排序从预设内迁移为全局配置 `appearance.font_order`，全局仅一份
- 变更：`/helps` 顶部统计由「总功能 / 分类」改为「总插件 / 已开启」
- 变更：`/helps` 左上角大标题固定为「Astrbot 插件面板」，移除英文副标题（搜索模式下副标题显示搜索结果）
- 新增：字体选择框自动收录系统字体库（Windows/macOS/Linux 常见字体目录），并支持直接输入字体名
- 变更：内置主题预设重构为 5 套 —— 新增 `Ocean Blue`（明亮清爽，默认）/ `Night Navy`（深色科技感）/ `Soft Mist`（莫兰迪低饱和）/ `Vivid Pop`（高饱和活力），保留 `zhenxun`；原 `Astrbot` 预设由 `Ocean Blue` 取代
- 变更：渲染模板支持深色主题 —— 卡片背景、边框、正文/标题文字、胶囊与高亮随预设切换明暗；分类标题栏与编号/计数徽章在浅色表面上保持深色文字，深色主题下详情页标题自动加亮
- 新增：内置主题预设系统 —— 内置「Astrbot」（天蓝青 × 深海蓝 × 奶油黄）与「zhenxun」（柔和马卡龙粉彩）两套配色，通过 `appearance.active_preset` 切换，未知预设自动回退默认
- 变更：插件行改为独立圆角行（编号徽章 + 插件名 + 灰色内部 ID）；支持 disabled 插件灰化展示
- 新增：禁用插件纳入 `/helps` 指令菜单（不再跳过），以固定灰化色板降低视觉权重 —— 浅灰卡片 `#F5F5F5` / 边框 `#E7E7E7`、灰粉编号徽章 `#D6C9CC`、浅灰插件名 `#B8B8B8`、次级文本 `#C8C8C8`、右侧「圆圈+斜杠（左下→右上）」禁用图标 `#B9A5A8`；尺寸/圆角/内边距/排列与普通插件完全一致，禁用态不改布局

## v0.2.4 (2026-08-16)

- 新增：插件详情页新增「事件监听」分区，展示该插件的非指令事件监听器；监听命令的事件处理器在描述命中「命令列表」模式时，自动提取命令关键词展示

## v0.2.3 (2026-08-16)

- 变更：搜索统一改为 `s` 子命令（如 `/helps s <关键词>`），直接后缀不再触发搜索
- 新增：`/helps` 增加中文别名 `/功能`、`/菜单`、`/帮助`
- 新增：`/events` 增加中文别名 `/事件`、`/事件监听`
- 新增：`/filters` 增加中文别名 `/过滤器`、`/过滤`

## v0.2.2 (2026-08-16)

- 变更：内置字体切换为 LXGW Neo XiHei，移除 Sarasa Gothic SC 与 Noto Color Emoji
- 变更：彩色 emoji 改用 Noto Color Emoji 精简子集，保留彩色图标渲染且不臃肿

## v0.2.1 (2026-08-16)

- 新增：插件全局连续编号（跨分类，不再每类重置），支持 `/helps <编号>` 按编号精确定位插件
- 变更：`/helps` 指令菜单改为分类卡片式面板（Zhenxun 风格）：按插件分类分组，每个分类一张彩色主题卡片，展示插件列表

## v0.2.0 (2026-08-15)

- 修复：适配新版 Typst 的 JSON 解析接口，将弃用的 `json.decode` 改为 `json(bytes(...))`，修复图片渲染失败的问题

## v0.1.1 (2026-01-22)

- 新增：开放颜色配置项：支持在配置面板自定义各视图配色
- 新增：额外的自定义字体目录支持，缺省值为 `.../data/plugin_data/astrbot_plugin_help_typst/fonts`
- 变更：渲染引擎改用 `typst-py` 新接口，移除对 `fonttools` 的依赖
- 变更：缓存存储由 `.hash` 文件切换为 AstrBot KV
- 修复：修复其他平台的兼容性问题

## v0.1.0 (2026-01-13)

- 新增：基于 Typst 的插件菜单渲染（指令 / 事件 / 过滤器）
- 新增：搜索能力：`helps/events/filters <关键词>`，附关键词高亮
- 新增：自定义字体（`.ttf/.otf/.woff2`）与外观预设系统
- 新增：自动撤回等待提示词
- 新增：插件黑名单
- 变更：迁移到 Pydantic V2
- 变更：优化视图与缓存逻辑，降低内存占用
- 变更：优化命令参数解析与生命周期钩子
- 修复：面对不规范的插件元信息增加 fallback 逻辑，提升健壮性
